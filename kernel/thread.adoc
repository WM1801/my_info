= Потоки 
:doctype: article
:title-page:
:toc:
:toc-title: Оглавление
:toclevels: 3
:sectnums:
:icons: font
:source-highlighter: rouge
:figure-caption: Рисунок
:table-caption: Таблица
:example-caption: Пример
:listing-caption: Листинг
:pdf-theme: default-with-fallback-font
:author: Владислав Богданов
:email: vla-bogdano@ya.ru
:revdate: {localdate}
:keywords: driver, thread, Module.symvers

toc::[]

== Введение 

*Windows* 

Приложение контейнер для потоков 

*Linux* 

Приложение = поток = процесс

*Классификация потоков* 

критериев много но рассмотрим по *отображению в ядро*

. 1:1 

. N:M 

. N:1 

=== 1:1 

Это поток созданный в любом процессе на любом уровне ОС управляется на прямую планировщиком ядра ОС. Он является полноценным потоком с точки зрения ОС. Именно эта технология используется в Linux. 

=== N:M 

Модель работает следующим образом, анализирует состояние системы и загрузку аппаратной части пк и выполняет следующие действия, она часть пользовательсикх потоков выполняет по средствам библиотек, то есть в реальности с точки зрения самой системы  все эти потоки являются одним процессом, а часть  потоков если ресурсы системы позволюят на уровне планировщика ОС. Этам модель является гибридной моделью и довольно трудно реализуема  так как требует требования к аппаратным ресурсам, примеры моделей обработка современной комп. графики, графические нейросети,  тензорный сети и тд. 

=== N:1 

Все потоки пользовательские представляются одним процессом на уровне ядра. То есть у нас есть некая библиотека которая сама управляет всеми запущеными ресурсами, но при этом все эти ресурсы сами по себе расходуют аппаратные ресурсы машины. Самая медленная модель. 


== POSIX 

В определенный момент в Linux  появился инструментарий который сответствует  стандарту POSIX. Это стандарт который в первую очередь  определяет  работу планировщика, на работы системы, сигналов, прерываний и тд. 

Как именно организовать работу систему? 

*Появились понятия:* 

. lightweight-process или потоки 
+
делят адресное пространство с породившим процессом, которые обладают связью с породившим процессом и обращаются к дескрипторам породившего их процесса и тд.

+
. process 

У него свое адресное пространство, свой набор дескрипторов


[TIP]  
====

Это для пользовательского пространства, в ядре иначе. 

В Linux всё работает по принципу строгой вложенности. 

Вот кто кого формирует:

Потоки собираются в Процесс (в ядре это называется Группа потоков).

Процессы собираются в Группу процессов.

Группы процессов собираются в Сессию.

Как это происходит при запуске (кто кого порождает):

. *Потоки в Процесс (Группа потоков)*: Один главный поток плодит другие потоки (через _pthread_create_ или _clone_). Вместе они образуют Группу потоков — то, что мы в обычной жизни называем *"одним процессом"*. У них одна память.

. *Процессы в Группа процессов*: Этот процесс может запустить дочерние процессы (через _fork_). Родитель и его дети образуют Группу процессов. Это нужно, например, чтобы разом убить всю цепочку команд по нажатию _Ctrl+C_.

. *Группы процессов в Сессия*: Все эти процессы и группы, которые работают в одном окне терминала (или одной SSH-сессии), автоматически попадают в одну общую Сессию. Это нужно, чтобы разом закрыть их все, если ты закроешь окно терминала.

*Короткая формула для запоминания:*

`Потоки в Процесс в Группа процессов в Сессия.`
====


*На уровне ядра любой поток является процессом.* 